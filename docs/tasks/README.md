# tgbot-module-summarizer — Tasks

> Active plans go here. One file = one task.
> After completion, compress essence into `../SDD-<topic>.md` and delete the task file.

## Rules

- PHP ^8.5, `declare(strict_types=1)`, English code/comments, LF only
- No git operations (no add/commit/push)
- Tests: Pest 4, `composer test` must pass before marking done
- Each task file: plan ref, scope, depends-on, checklist, status

